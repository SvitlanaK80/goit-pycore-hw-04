def get_cats_info(path):
    """
    Функція зчитує файл із котами та повертає список словників.
    
    Аргументи:
    - path: шлях до файлу з даними про котів.
    
    Повертає:
    - Список словників, де кожен словник містить 'id', 'name', 'age'.
    """
    cats_list = []  # Створюємо порожній список для збереження інформації про котів

    try:
        # Відкриваємо файл для читання (utf-8 для підтримки кирилиці)
        with open(path, "r", encoding="utf-8") as file:
            # Читаємо всі рядки у файлі
            for line in file:
                # Видаляємо зайві пробіли/переноси рядків та розбиваємо рядок по комі
                parts = line.strip().split(",")
                
                # Перевіряємо, чи є всі три частини (id, name, age)
                if len(parts) == 3:
                    cat_id, name, age = parts  # Розпаковуємо значення
                    
                    # Створюємо словник для кота та додаємо його у список
                    cats_list.append({
                        "id": cat_id,
                        "name": name,
                        "age": int(age)  # Конвертуємо вік у число
                    })
        
        return cats_list  # Повертаємо список котів

    except FileNotFoundError:
        print("Помилка: Файл не знайдено.")
        return []  # Повертаємо порожній список
    except Exception as e:
        print(f"Сталася помилка: {e}")
        return []  # Повертаємо порожній список у разі інших помилок

# 🔹 Виклик функції
cats_info = get_cats_info("cats.txt")  # Вкажи шлях до файлу з котами
print("Інформація про котів:", *cats_info, sep="\n")
